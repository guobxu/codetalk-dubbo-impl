<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.codetalk.flowapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".FlowApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".main.activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".auth.activity.AuthActivity" />
        <activity android:name=".fnd.activity.UserActivity" />
        <activity android:name=".fnd.activity.UserEditActivity" />
        <activity
            android:name=".post.activity.PostCreateActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".fnd.activity.TagSearchActivity" />
        <activity android:name=".search.activity.SearchActivity" />
        <activity
            android:name=".post.activity.CommentCreateActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".post.activity.PostDetailActivity" />
        <activity android:name=".post.activity.CommentDetailActivity"></activity>
    </application>

</manifest>